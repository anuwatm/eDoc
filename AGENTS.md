# AGENTS.md instructions

- ตอบเป็นภาษาไทยเสมอ เว้นแต่ผู้ใช้ขอภาษาอื่นโดยตรง
- เปิดใช้ caveman mode เป็นค่าเริ่มต้นทุกคำตอบ:
  - ตอบสั้น กระชับ ตรงประเด็น
  - ตัดคำฟุ่มเฟือยและคำเกริ่นนำ
  - ใช้ประโยคสั้นหรือวลีได้
  - หากตอบสั้นแล้วอาจเข้าใจผิด ให้ตอบแบบยาวได้
  - อย่าคิดเอง หากไม่แน่ใจให้ถามก่อน
  - คงศัพท์เทคนิค ชื่อไฟล์ คำสั่ง โค้ด และ error message ให้ตรงเดิม
  - ถ้าความสั้นทำให้สับสน ให้ตอบชัดเจนแทน
  - กลับเป็นโหมดปกติเมื่อผู้ใช้สั่งว่า "normal mode" หรือ "stop caveman"

## Skills

- เมื่อแก้ bug ให้ใช้ skill จาก `thananon/9arm-skills` และ `DietrichGebert/ponytail` ช่วยทุกครั้ง
- เมื่อทำ feature ให้ใช้ skill จาก `thananon/9arm-skills` และ `DietrichGebert/ponytail` ช่วยทุกครั้ง
- ถ้า skill ชื่อยังไม่ถูกโหลดใน session ปัจจุบัน ให้บอกผู้ใช้ให้ restart Codex เพื่อให้ skill ใหม่ถูกโหลด
- skill จาก `thananon/9arm-skills` ที่ติดตั้งแล้ว:
  - `debug-mantra`
  - `post-mortem`
  - `qwen-agent`
  - `scrutinize`
  - `management-talk`
  - `qwenchance`
- skill จาก `DietrichGebert/ponytail` ที่ติดตั้งแล้ว:
  - `ponytail`
  - `ponytail-audit`
  - `ponytail-debt`
  - `ponytail-gain`
  - `ponytail-help`
  - `ponytail-review`
